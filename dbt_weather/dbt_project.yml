name: weather_dw
version: '1.0.0'
config-version: 2

profile: weather_dw

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  weather_dw:
    +materialized: view
    staging:
      +schema: raw
      +materialized: view
    intermediate:
      +schema: intermediate
    marts:
      +schema: mart
